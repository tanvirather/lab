echo "Your text here" | festival --tts
sudo apt install rhythmbox rhythmbox-plugins
text2wave audio.txt -o output.wav

# youtueb video
```sh
sudo apt update
sudo apt install yt-dlp ffmpeg
yt-dlp --update-to nightly

sudo snap install yt-dlp --edge
/snap/bin/yt-dlp "https://www.youtube.com/watch?v=JyB3Md1GcWo"
```

sudo apt update && sudo apt install festival
sudo modprobe snd-dummy
sudo usermod -aG audio $USER
