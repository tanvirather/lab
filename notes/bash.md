# Bash Scripts
```sh
rsync --recursive --delete <FROM> <TO> --exclude '<EXCLUDE_1>' --exclude '<EXCLUDE_2>' # Syncs files recursively, deletes extra files at destination, excludes specified patterns
```
