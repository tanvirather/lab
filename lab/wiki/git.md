# git commands
```sh
git add . && git commit -m "Your commit message" # stage all changes and commit with message
git commit -am "Your commit message" # stage tracked files and commit with message
git push # push changes
```
